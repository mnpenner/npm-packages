update `eslint.config.ts`. add a list of "ignore packages" somewhere near the top where i can add a list of old packages that i'm not ready to fix. add these packages in there for now

- yamake
- svg2fonts
- svg-to-react-webpack-loader
- rollup-plugins
