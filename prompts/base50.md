Freshen packages/base50

- Switch from rollup to rolldown
- Update package.json; the main build script needs to be called "build"
- Delete all the linting and typechecking stuff; that will now be handled by the root config


Do the same for packages/uri-template
