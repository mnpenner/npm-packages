scripts/test-coverage.ts should be updated to update-readme.ts

We should add some checkmarks/xs to indicate its current status.

Maybe just 4 icons in 1 cell:

✓ lint
✓ typecheck
✗ formatted correctly
✓ unit tests passing



Update the docs col to link docs on npmx.dev if the package has types. e.g. https://npmx.dev/package-docs/@mpen/base50 use "npmx" as the link.

If the package also has typedocs, use "TypeDoc" as the link


make publish.ts automatically call update-readme.ts


