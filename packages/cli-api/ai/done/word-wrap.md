Long --option descriptions need to be word-wrapped.

If the description doesn't fit on one line, given the terminal width, it should drop down and indent, and wrap. We'll need an extra line break between options as well.

jj supports a short option description with -h and a longer description with --help. It appears the short descrip is a prefix of the long descrip.
