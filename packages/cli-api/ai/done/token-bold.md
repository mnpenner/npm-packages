similar to formatPath (in options.ts), make formatToken. it should make --options bold when supported, or put backticks around it when not supported.

Also add word wrapping for block errors.

block errors should not be indented when color is not enabled
