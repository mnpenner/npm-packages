{
  "name": "js-serialize",
  "version": "0.6.0",
  "author": "Mark Penner <npm@mpen.ca>",
  "devDependencies": {
    "bun-types": "^1.2.15",
    "npm-run-all": "^4.1.5",
    "typescript": "^5",
    "xregexp": "^3.1.1"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "description": "",
  "engines": {
    "bun": ">= 1.2",
    "node": ">= 14"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "packageManager": "bun@1.2",
  "private": false,
  "scripts": {
    "test": "bun test src",
    "build": "esbuild --bundle src/index.ts --outdir=dist --sourcemap --platform=node --target=node14",
    "bundle:types": "tsc --build --force",
    "bundle:esm": "NODE_ENV=production bun build src/index.ts --outdir=dist --format=esm",
    "bundle": "rm -rf dist && run-p 'bundle:*'"
  },
  "sideEffects": false,
  "type": "module"
}
