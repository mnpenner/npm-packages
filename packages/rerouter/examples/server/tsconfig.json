{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "lib": ["DOM", "DOM.Iterable", "ESNext"],
        "types": ["bun", "node"]
    },
    "include": ["serve-dist.ts"],
    "exclude": ["**/*.test.*"]
}
