{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "lib": ["DOM", "DOM.Iterable", "ESNext"],
        "types": ["node"]
    },
    "include": ["**/*"],
    "exclude": ["**/*.test.*"]
}
