{
    "private": true,
    "packageManager": "bun",
    "workspaces": [
        "packages/*"
    ],
    "type": "module",
    "devDependencies": {
        "@types/bun": "latest",
        "npm-run-all": "^4.1.5",
        "typescript": "^5.9.3"
    },
    "scripts": {
        "build:react-router": "bun --cwd packages/react-router run build",
        "build:server-router": "bun --cwd packages/server-router run build",
        "build": "run-s build:react-router build:server-router",
        "gen:react-router": "bun --cwd packages/react-router run gen",
        "gen:server-router": "bun --cwd packages/server-router run gen",
        "gen": "run-s gen:react-router gen:server-router",
        "test:types": "tsc -b tsconfig.json",
        "test:impl": "bun test packages/",
        "test:gen": "run-s gen test:impl",
        "test": "run-s test:types test:gen"
    }
}
